package vn.vietduc.carehubbackend.questiongeneration.entity.enums;

/** Whether a retake keeps the target's paper or rotates through the batch. */
public enum ExamAssignmentRetakeVariantPolicy {
    KEEP_VARIANT,
    ROTATE_VARIANT
}
